# Opus5への引き継ぎ — 2026-09-10

## 最初に読むこと

作業場所は `C:\Users\situz\Documents\ChatGPT\Astragemes`。このフォルダーがゲーム本体です。同じPCならZIPを展開する必要はありません。`AGENTS.md`、本書、`work/CURRENT-SNAPSHOT.json`、`work/CURRENT-STATE.md` の順で現在地を確認してください。CURRENT-STATEは追記形式なので、古い未完メモより末尾の変更を優先します。

ユーザーはサブエージェントを禁止しています。直接作業してください。今回の依頼は引き継ぎ資料の提供であり、ゲームへの追加変更はしていません。

## 保存版と採用状況

- 最新保存：`dist/history/ASTRA-OVERDRIVE-redrawn-idle-20260910-172606.zip`
- 保存日時：2026-09-10 17:26:06 JST
- ZIP SHA-256：`85fa64b50263b4a917657f773f4c0c2f505717e7681a9800d8786b21ad4cd284`
- 保存ファイル62個。各ファイルのSHA-256はCURRENT-SNAPSHOTにあります。
- 配布用：`dist/ASTRA-OVERDRIVE.zip`。引き継ぎZIPには最新保存ZIPも同梱します。

**最新の待機絵は実装・検証済みですが、ユーザーの見た目の承認はまだありません。** 直前の合成リグ待機は「かっこ悪すぎ」と明確に却下されました。`front-facing-idle-20260910-171549` 保存版を採用版として戻さないでください。次にユーザーの評価を受けてから必要な修正を進めてください。

現在の工場背景、青・アイボリーの主人公、シアンのスカーフ、敵の機械兵デザイン、UI、操作感を維持します。参考の赤いキャラへ置き換える依頼ではありません。簡易図形の画面になった場合は、先に4173サーバーと画像ロードを確認してください。過去の「別ゲームになった」原因は素材配信の停止でした。

## 直近の待機モーション

ユーザーの意図は、胸をこちらへ向け、足を左右に開き、顔は進行方向を向く自然な構えです。継ぎはぎの胸・腰・腕と深いがに股が却下されたため、一体の待機絵を描き直しました。

- 新素材：`assets/player-idle-v2.png`、1254×1254 RGBA。生成時の透過をそのまま保持。
- 実装：`assets/run-rig-v6.js` の `loadIdle()` / `drawIdle()` / `idleReady`。
- 元画像の領域 x176,y160,w803,h986 を高さ48pxで表示。足元の原点は元画像(654,1146)。腰y700で上下2領域に分け、上体を±0.35px呼吸させ、下半身の描画高で足元を固定します。
- `draw()` は `o.idle && idleReady` の場合、専用一体絵へ分岐。左右反転あり。
- 旧 `standingPose()` や待機 `armPose()` の数値は補助計算・フォールバックとして残っています。これだけを調整しても通常の待機絵は変わりません。
- 待機中の発射・チャージは専用待機絵を使わず、既存の空中射撃と同じスプライトへ切り替わります。
- 素材の生成プロンプトと参照：`work/IDLE-ART-20260910.md`。
- 最新実画面：`qa/saber-legs-open/live/idle-in-game.png`。
- 左右の呼吸比較：`qa/saber-legs-open/idle-redrawn.png`。

## 維持する操作・戦闘・音

- キャラクター本人の右手＝手袋とセイバー、左腕＝バスター。左右向きによる画面上の左右と混同しないこと。
- 左クリック／J：押している間チャージし、離したときだけ1発。チャージ開始時に通常弾が勝手に出る挙動は修正済み。
- 通常弾威力1、チャージショット3。チャージ判定0.55、増加0.85/秒、発射間隔0.18秒。
- 右クリック／K：3段セイバー、範囲72px。1・2段各4.5、3段は2.8125×4回＝11.25。全段合計20.25。
- 各段0.40秒。先頭0.24秒は旧0.32秒版のテンポを維持し、振り抜き後を0.16秒へ延長。「もっと余裕のある感じ」の依頼は実装済み。
- `combat.saberPhase()` で時間から描画位相へ変換。直接 `1-time/.32` 等へ戻さないこと。
- 次段入力は0.12秒以降、1回分を予約。自動連打しない。
- セイバーは敵弾も消去。開始0.08〜0.24秒、前方72px・上下55px＋弾半径、プレイヤー被弾より先に処理。
- セイバーSEはユーザーが「良い感じ」と承認済み。`assets/saber-swing.wav` と `assets/saber-sound.js`。提供se.mp4から作った約0.56秒の音。動き調整のついでに変更しない。
- チャージ音：提供音源の `assets/charge-hold.mp3` / `assets/charge-fire.mp3` と埋め込み `charge-sounds.js`。長押しループ、解放で停止・発射。
- 全体音量初期値0.4。ステージ1はユーザー提供BGM。ポーズ／リザルト時のBGM継続も維持。

## セイバーモーションの現在地

主実装 `assets/saber-rig.js`。参考GIFを32コマに分解して、頭上斬り→腰の横薙ぎ→早めの斬り下ろしと低姿勢保持を調整してきました。顔は常に進行方向で、カメラ目線の追加演出は不要です。

- 一・三段の半月は `smearKeys` の手元相対輪郭。汎用の円弧に戻さず、参考の幅・傾き・消失順序を保つこと。
- 二段目は胴を開く扁平な横薙ぎ。胸だけ `saber-turn-atlas.png` のy510以降を使い、ヘルメットは既存横顔。段末で胴のひねりを0へ戻し三段目へ接続。
- 三段目は位相0.51までに低い姿勢へ到達し0.92まで保持。
- `plant()` が大切です。太もも12.5px＋すね15pxに合わせ、腰が低いと後ろ足を送って膝が床へ折れるのを防止しています。腰・足首だけ独立に変更すると破綻します。
- 一段目は腰を上げ、前傾と前足の張り出しを減らし、左腕を後ろへ。三段目は上体を少し起こし、右手を前脚近く、左腕を体の前へ畳むよう修正済み。
- 完全なGIF模写の達成は未判定。自動テストの合格は見た目の完全一致を意味しません。

詳しくは `work/SABER-32-FRAME-REVIEW-20260910.md` と `qa/saber-faithful/trace/body-landmarks.json`。最新の動作動画は `qa/saber-faithful/combo-preview.mp4`。過去の引き継ぎや監査には既に解消した項目があるため、この文書とCURRENT末尾を優先します。

## 起動と検証

```powershell
Set-Location 'C:\Users\situz\Documents\ChatGPT\Astragemes'
node server.cjs
```

ゲームは `http://127.0.0.1:4173/`。既にサーバーが起動していれば二重起動不要。データや正常な画像を上書きする前にHTTPと画像ロードを確認します。

```powershell
npm test
node qa/idle-redrawn-verify.cjs
node qa/saber-legs-open/idle.cjs idle-redrawn
node qa/saber-legs-open/idle-live.cjs
```

引き継ぎ時にnpm testを再実行して全成功。engine testsと各組11・7・4・16・18項目、fail0。ログ：`work/handoff-20260910-tests.log`。

最新待機の描画検証は左右202姿勢、新画像のみを2領域で描画、足元誤差0、継ぎ目誤差約3.6e-15、呼吸移動幅0.7px、エラー0。`qa/idle-redrawn-verification.json`。これは前回実装時の検証結果です。

セイバーを変更した場合の検証：

```powershell
node qa/saber-motion-reference/verify.cjs
node qa/saber-legs-open/verify.cjs
node qa/saber-legs-open/finisher.cjs
node qa/saber-faithful/compare-reference.cjs
node qa/saber-faithful/preview.cjs
```

前回結果：左右606描画、脚1203姿勢、実右クリック3段のダメージ20.25、胸だけの切り出しを確認。古いQAには0.32秒や255msの固定待ちが残るものがあります。現在0.40秒なので、その仮定を確認せず不具合と判定しないこと。比較32コマは動作の対応点を合わせたもので、元GIFの厳密な絶対時間との一致ではありません。

QAのPlaywrightパスは同じPCの `C:/Users/situz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright`、Chromeは `C:/Program Files/Google/Chrome/Application/chrome.exe`。別環境では要設定。Pythonは `.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe`。Windowsでは日本語ファイルをUTF-8指定で読み書きしてください。

## 引き継ぎZIPと次の保存

ZIPはゲームの62ファイル、現在地記録、本書、AGENTS、最新保存ZIP、必要な検証スクリプト・画像・動画、参考GIFと待機画像を収録。過去の全バックアップ、依存ランタイム、大容量の録画一式は省略。`HANDOFF-FILES-SHA256.json` に同梱ファイルのハッシュを記録しています。

同じPCなら今のフォルダーから続行し、ZIPで現状を一括上書きしないでください。別フォルダーへ展開した場合はCURRENT-SNAPSHOTの絶対パスを元のPC用として読み替えます。

次の実装前に最新保存のZIPと各ファイルのハッシュを確認。差分があればユーザーや他モデルの変更を退避してから作業します。完了時は新しい名前の履歴ZIPを作成し、配布ZIPとCURRENTの2ファイルを更新します。過去の保存版は残してください。
